import { CorreuDirective } from './correu.directive';

describe('CorreuDirective', () => {
  it('should create an instance', () => {
    const directive = new CorreuDirective();
    expect(directive).toBeTruthy();
  });
});
